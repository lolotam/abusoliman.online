# دليل استكشاف الأخطاء وإصلاحها
## أبوسليمان للمحاسبة - نظام إدارة نقاط البيع

---

## 🚨 مشاكل تسجيل الدخول وفقدان البيانات

### المشكلة الأساسية
عند إعادة تشغيل التطبيق، قد تواجه:
- عدم القدرة على تسجيل الدخول
- فقدان البيانات المضافة حديثاً
- رسائل خطأ في المصادقة
- عدم تحميل البيانات بشكل صحيح

---

## 🔍 التشخيص السريع

### 1. فتح وحة التحكم (Developer Console)
```
اضغط F12 أو Ctrl+Shift+I
انتقل إلى تبويب "Console"
```

### 2. تشغيل التشخيص التلقائي
```javascript
runDiagnostic()
```

### 3. الإصلاح التلقائي
```javascript
autoFix()
```

---

## 🛠️ الحلول المرحلية

### الحل الأول: إعادة تعيين كلمة المرور
```javascript
// في وحة التحكم
const settings = db.getTable('settings');
settings.password = db.hashPassword('123');
db.setTable('settings', settings);
console.log('تم إعادة تعيين كلمة المرور إلى: 123');
```

### الحل الثاني: إنشاء المستخدم الافتراضي
```javascript
// في وحة التحكم
createDefaultAdmin();
console.log('تم إنشاء المستخدم الافتراضي: admin / 123');
```

### الحل الثالث: مسح الجلسة التالفة
```javascript
// في وحة التحكم
localStorage.removeItem('currentUser');
sessionStorage.clear();
location.reload();
```

---

## 🔧 الحلول الشاملة

### 1. إصلاح قاعدة البيانات
```javascript
// إصلاح البيانات التالفة
db.validateAndRepairData();

// إعادة تهيئة كاملة
db.initializeDatabase();
db.completeInitialization();
```

### 2. مسح البيانات التالفة
```javascript
// مسح البيانات التالفة فقط (يحافظ على البيانات السليمة)
clearCorruptedData();
```

### 3. إعادة تعيين كاملة (الحل الأخير)
```javascript
// تحذير: سيتم مسح جميع البيانات
localStorage.clear();
sessionStorage.clear();
location.reload();
```

---

## 📋 بيانات الدخول الافتراضية

### النظام الجديد (مستحسن)
- **اسم المستخدم:** admin
- **كلمة المرور:** 123

### النظام القديم (احتياطي)
- **كلمة المرور فقط:** 123

---

## 🔍 فحص حالة النظام

### فحص المستخدمين
```javascript
const users = db.getTable('users');
console.log('المستخدمين:', users);
```

### فحص الإعدادات
```javascript
const settings = db.getTable('settings');
console.log('الإعدادات:', settings);
```

### فحص الجلسة الحالية
```javascript
const currentUser = localStorage.getItem('currentUser');
console.log('الجلسة الحالية:', currentUser ? JSON.parse(currentUser) : 'لا توجد جلسة');
```

---

## ⚠️ أسباب المشاكل الشائعة

### 1. مشاكل المتصفح
- **امتلاء localStorage:** حد التخزين 5-10 MB
- **إعدادات الخصوصية:** منع التخزين المحلي
- **وضع التصفح الخاص:** لا يحفظ البيانات

### 2. مشاكل البيانات
- **تلف JSON:** بيانات غير صالحة في localStorage
- **تضارب الإصدارات:** خلط بين النظام القديم والجديد
- **فقدان المفاتيح:** حذف جداول مهمة

### 3. مشاكل المصادقة
- **تضارب كلمات المرور:** طرق تشفير مختلفة
- **جلسات تالفة:** بيانات مستخدم غير صحيحة
- **مستخدمين مفقودين:** عدم وجود المستخدم الافتراضي

---

## 🚀 الوقاية من المشاكل

### 1. النسخ الاحتياطية التلقائية
```javascript
// تفعيل النسخ الاحتياطية التلقائية
setInterval(() => {
    db.createBackup();
}, 30 * 60 * 1000); // كل 30 دقيقة
```

### 2. مراقبة حجم البيانات
```javascript
// فحص حجم البيانات
let totalSize = 0;
for (let key in localStorage) {
    totalSize += localStorage[key].length;
}
console.log('حجم البيانات:', (totalSize / 1024 / 1024).toFixed(2), 'MB');
```

### 3. تصدير البيانات دورياً
```javascript
// تصدير البيانات
exportData();
```

---

## 🆘 الدعم الفني

### إذا استمرت المشاكل:

1. **تشغيل التشخيص الشامل:**
   ```javascript
   runDiagnostic()
   ```

2. **حفظ تقرير الأخطاء:**
   ```javascript
   console.save = function(data, filename) {
       const blob = new Blob([data], {type: 'text/plain'});
       const url = URL.createObjectURL(blob);
       const a = document.createElement('a');
       a.href = url;
       a.download = filename;
       a.click();
   };
   
   // حفظ تقرير الأخطاء
   console.save(JSON.stringify(diagnostic.runFullDiagnostic(), null, 2), 'error-report.json');
   ```

3. **إعادة تعيين كاملة مع حفظ البيانات المهمة:**
   ```javascript
   // حفظ البيانات المهمة
   const backup = {
       products: db.getTable('products'),
       customers: db.getTable('customers'),
       suppliers: db.getTable('suppliers')
   };
   
   // مسح النظام
   localStorage.clear();
   
   // إعادة التهيئة
   location.reload();
   
   // استعادة البيانات (بعد إعادة التحميل)
   // db.setTable('products', backup.products);
   // db.setTable('customers', backup.customers);
   // db.setTable('suppliers', backup.suppliers);
   ```

---

## 📞 معلومات الاتصال

للحصول على دعم إضافي، يرجى الاتصال بفريق الدعم الفني مع تقرير الأخطاء المُصدر.

---

**ملاحظة:** هذا الدليل يغطي المشاكل الأكثر شيوعاً. للمشاكل المعقدة، يُنصح بالاتصال بالدعم الفني.
